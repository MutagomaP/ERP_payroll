# Application Configuration
spring.application.name=ERP Payroll Management System
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/employee_db
spring.datasource.username=postgres
spring.datasource.password=1994
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Spring Cloud Config (Disabled)
spring.cloud.config.enabled=false
spring.config.import=optional:configserver:,optional:classpath:application-local.properties

# JWT Configuration
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=3600000

# Swagger Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.tagsSorter=order
springdoc.swagger-ui.operationsSorter=alpha

# Mail — Gmail requires an App Password (not your normal login password).
# Create one: Google Account → Security → 2-Step Verification → App passwords
# Set MAIL_PASSWORD in the environment, or use application-local.properties (see application-local.properties.example)
app.mail.enabled=true
app.mail.from=mutapatie250@gmail.com
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mutapatie250@gmail.com
spring.mail.password=czsrzeuivxptbszx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.rwanda.gov.erp=DEBUG
